name: web-firecrawl
version: 1.0.0
description: "Firecrawl web search + content extraction. Supports keyless cloud, direct API, and Clover-hosted tool-gateway routing for subscribers."
author: cloverc1
kind: backend
provides_web_providers:
  - firecrawl
