name: openai
version: 1.0.0
description: "OpenAI image generation backend (gpt-image-2). Saves generated images to $CLOVER_HOME/cache/images/."
author: cloverc1
kind: backend
requires_env:
  - OPENAI_API_KEY
