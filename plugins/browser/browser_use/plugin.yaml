name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed Clover tool gateway. Also powers the 'Clover Subscription' UX flow that bills usage to a Clover subscription."
author: CloverCognition
kind: backend
provides_browser_providers:
  - browser-use
